'use client';

import { motion } from 'framer-motion';

const Shining = () => {
  return (
    <div className='absolute -inset-0.5 z-20 rounded-lg hover:shadow-primary'></div>
  );
};

export default Shining;
